/*
 * Créez deux fonctions:
 * 
 * 1. find_max(int count, ...) : trouve le maximum
 * 2. find_min(int count, ...) : trouve le minimum
 * 3. find_minmax(int count, ...) : utilise va_copy pour 
 *    appeler les deux fonctions ci-dessus avec la même liste
 * 
 * Exemple:
 * find_minmax(5, 10, 3, 45, 2, 18);
 * → Min: 2, Max: 45
 * 
 * Utilisez va_copy() pour dupliquer la liste d'arguments
 */